# 本地环境变量样板 —— 复制为 .env.local 后按需启用
# (Next.js 会自动加载 .env.local；NEXT_PUBLIC_* 会被注入到客户端 bundle)

# 会场组件库降级开关：设为 "true" 时，编辑器「会场」tab 绕过
# /api/venue-components 接口，直接读 FALLBACK_VENUE_COMPONENTS 常量
# （7 分组 × 2 卡 = 14 张纯 SVG mock）。
# 典型使用场景：
#   - 本地 DB 没有 venue_components 数据，想直接看编辑器插入 / 导出
#   - demo 环境临时故障，需要一个不依赖 API 的保底展示
# 未设置（或任何非 "true" 值）= 走实时 API，这是默认行为。
# NEXT_PUBLIC_USE_MOCK_VENUE_COMPONENTS=true
